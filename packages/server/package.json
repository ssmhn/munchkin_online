{
  "name": "@munchkin/server",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate"
  },
  "dependencies": {
    "@munchkin/game-engine": "workspace:*",
    "@munchkin/shared": "workspace:*",
    "@prisma/client": "^7.4.2",
    "ioredis": "^5.10.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "prisma": "^7.4.2",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
